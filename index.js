const fn = () => {
    const miString = "soy un string!"
    let b = miString + " motivado"
    console.log(b)    
}

// 
fn()

const clickeado = () => {
    let a = "piojo motivado"

    console.log(a)
}